# Agent skill symlinks. The real files live in .agents/skills/ and are committed;
# `npx skills` recreates these links with absolute paths, which are per-machine.
.claude/skills/opentui

# Dependencies and build output
node_modules/
dist/

# Local runtime state
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log

# Raw 2026 reference archives (7 MB). Not committed; fixtures/2026/*.zip are derived
# from them by tools/fixtures/derive-reference.ts. Re-fetch with the URLs in
# fixtures/README.md.
fixtures/source-2026-reg/

# Local mirrors of previous elections, built by tools/mirror/build.ts.
mirror/
